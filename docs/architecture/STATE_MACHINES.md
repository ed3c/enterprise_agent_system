# State Machines

| ID | Owner | Transition | Terminal evidence ceiling |
|---|---|---|---|
| `SM-EAS-SOURCE` | EAS-C/profile C0 | `SOURCE_REGISTERED → PAGE_LOCATORS_CAPTURED → REQUIREMENTS_ASSIGNED → CONTRADICTIONS_ROUTED → PROFILE_CONTRACT_READY` | source graph |
| `SM-EAS-CONTRACT` | EAS-C/C1 | `UNBOUND → SOURCE_SUBJECT_BOUND → OWNER_PLANE_BOUND → CONTRACT_IDS_BOUND → EVIDENCE_LANES_DECLARED → CONSUMER_READY` | contract shape |
| `SM-EAS-TECH-LEAD` | EAS-K | `REQUEST_BOUND → DAG_COMPILED → PACKETS_EMITTED → LEASES_ADMITTED → CANDIDATES_RECEIVED → GATES_APPLIED → REDUCED` | deterministic plan/reduction |
| `SM-EAS-PROJECTION` | EAS-A | `REFERENCE_DECLARED → CAPABILITY_RESOLVED → REVISION_CAPTURED → DIGESTED → RENDERED → CURRENT|STALE|REFUSED` | advisory projection |
| `SM-COMPACTION` | Bettor/runtime-env | `BUDGET_OBSERVED → SAFE_BOUNDARY → SNAPSHOT_VALIDATED → COMMITTED → RECONSTRUCTED → RECOVERY_PROBED → RESUMED|ROLLED_BACK|HUMAN` | local/physical owner receipt |
| `SM-STEERING` | Agent Shield/runtime-env | `CAPABILITIES_DISCOVERED → SYNC_POINT → ACTION_PROPOSED → POLICY_CHECKED → CHECKPOINT|TOOL|CANCEL|NO_ACTION|HUMAN → RECEIPT` | provider/runtime receipt |
| `SM-EVIDENCE` | Truth Verify/OpenWiki/Bettor | `CLAIM_BOUND → EXACT_SUBJECT → READ_BACK → PHYSICAL_GATES → SEMANTIC_REVIEW → SUPPORTED|REFUTED|CONFLICTED|UNVERIFIABLE` | lane-separated verification |
| `SM-PROVENANCE` | policy owners/Human | `SUBJECT_DISCOVERED → VERSION_CAPTURED → LINEAGE_RESOLVED → OBLIGATIONS_EVALUATED → POLICY_CANDIDATE|BLOCKED|HUMAN_REVIEW` | no automated legal claim |
| `SM-DISCOVERY` | Bettor/Agent Shield/Human | `RADAR → SOURCE_CAPTURED → PRECHECK → FIT → ISOLATED_CANDIDATE → CONTROLS/BENCHMARK → SHADOW → ADMIT|REJECT|DEFER` | Human admission separate |
| `SM-INGRESS-EFFECT` | Bettor/Agent Shield | `RAW_EVENT → AUTH/REPLAY → DEDUPE → DURABLE_INBOX → WORKFLOW → WRITE_INTENT → EFFECT_RESERVED → EXECUTED → READBACK → COMMITTED|UNKNOWN|COMPENSATE|HUMAN` | external-effect literal |
| `SM-SHADOW` | EAS-E/profile E | `PUBLIC_SUBJECT_BOUND → APPLICABILITY → DELTA → READ_BACK → LANES_RECONCILED → GLOBAL_OBJECTIVE → FINDINGS_ROUTED → ADMIT_FOR_REVIEW|BLOCK` | evaluator only |
| `SM-CLOSURE` | profile X/EAS-X | closure ladder from source to operated rollback | exact lane receipts |
| `SM-DOCS` | profile D/EAS-D | `INPUTS_PINNED → ROUTES_RENDERED → LINKS/STACK CHECKED → HANDOFF_READY` | documentation only |
| `SM-HANDOFF` | #14 | `QUEUE_SUBJECT_BOUND → ACTIVE_SELECTED → CAPABILITIES_REBOUND → COMMANDS_MATERIALIZED → EXECUTED → RECEIPT/CLEANUP → RECONCILED → NEXT|HUMAN|COMPLETE|BLOCKED` | queue shape != execution |
