# AGENTS.md — Agent Thinking Inception profile

## Read order

1. repository root `AGENTS.md` when it exists;
2. root `README.md`, `ARCHITECTURE.md`, and `CONTEXT.md`;
3. `contracts/control-plane/README.md` and exact schemas;
4. this `README.md`;
5. `source/source-subject.json` and `source/source-map.json`;
6. `requirements/requirements.json` and `requirements/contradictions.json`;
7. the exact profile contract/example/test;
8. the owning issue, branch/PR head, commit/tree, gate receipt, and evidence lane.

## Laws

- The PDF is `SOURCE_PROPOSAL`, never runtime truth.
- Do not publish the source bytes or its private/session-specific URL.
- Keep the full requirement denominator; failed, blocked, conflicted, skipped, and unexercised items remain visible.
- One interface and canonical state have one owner repository.
- Fixed percentages, token budgets, model/provider names and license labels are policy candidates, not universal facts.
- Conversation history is not durable Domain State; summary text is not an admitted checkpoint.
- Complete assistant/tool/result transactions must remain atomic.
- Provider steering capabilities must be observed; unsupported capability is `ABSENT`.
- A deterministic path/AST gate and a probabilistic judge are distinct evidence lanes.
- Google Docs/Sheets are advisory projections; GitHub issues/PRs are delivery metadata; machine records and exact receipts remain canonical.
- No Worker, provider, model, issue state, PR merge, CI run or document may self-promote Human/release state.
- Root/index/shared paths remain read-only for this atom; EAS-D owns final convergence.

## Writable paths

```text
profiles/agent-thinking-inception/source/**
profiles/agent-thinking-inception/requirements/**
profiles/agent-thinking-inception/contracts/**
profiles/agent-thinking-inception/examples/**
profiles/agent-thinking-inception/tests/**
profiles/agent-thinking-inception/prompts/00-*
profiles/agent-thinking-inception/prompts/01-*
profiles/agent-thinking-inception/README.md
profiles/agent-thinking-inception/AGENTS.md
```

## Stop conditions

Stop on stale source digest, missing page locator, duplicate requirement, missing owner/issue/State Machine, private locator egress, false evidence promotion, duplicate owner for one interface, unsupported capability represented as present, omitted blocker, or any attempt to mutate root docs, owner implementations, the canonical reducer, the Local Handoff queue, merge/release state, permissions, visibility or licenses.

## Completion packet

Return exact base/head/tree, source digest, requirement and contradiction counts, changed paths, gate/selftest results, claims not proven, owner issue packets, blocked live/Human lanes, rollback subject, and the next EAS-K authority.
